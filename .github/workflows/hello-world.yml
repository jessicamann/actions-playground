name: Hello World

on: 
  workflow_call:
    inputs:
      run_id:
        required: false
        type: number
      attempt_number:
        required: false
        type: number
      timeout_minutes:
        required: false
        type: number

jobs:
  Hello:
    runs-on: ubuntu-latest
    steps:
      - name: Greetings 👋🏻
        run: echo "Hellooooooooo"
      - name: Inputs
        run: |
          echo "run_id: ${{inputs.run_id}}"
          echo "attempt_number: ${{inputs.attempt_number}}"
          echo "timeout_minutes: ${{inputs.timeout_minutes}}"