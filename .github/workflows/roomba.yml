name: Approval Waiting Roomba

on:
  workflow_call:
  push:
    branches:
      - roomba

concurrency: roomba_workflow

jobs:
  Cleanup-Time:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Cleaning up earlier workflows that is still waiting for approval 🧹"
      - name: Retrieve earlier workflows that are still pending review
        uses: actions/github-script@v6
        with:
          script: |
            const response = await github.rest.actions.listWorkflowRunsForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              status: "waiting"
            })
            console.log(Object.keys(response.data))
            console.log(Object.keys(response.data.totalCount))
            console.log(Object.keys(response.workflow_runs[0]))
