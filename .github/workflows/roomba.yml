name: Approval Waiting Roomba

on: workflow_call

concurrency: roomba_workflow

jobs:
  Cleanup-Time:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Cleaning up earlier workflows that is still waiting for approval 🧹"
      - name: Looking for earlier workflows still in waiting
        id: look_for_deployments_pending_review
        uses: actions/github-script@v6
        with:
          script: |
            console.log(`owner: ${context.repo.owner}`)
            console.log(`repo: ${context.repo.repo}`)
            console.log(`run_id: ${{github.run_id}}`)
            # const {data: workflows} = await github.rest.actions.getPendingDeploymentsForRun({
            #   owner: context.repo.owner,
            #   repo: context.repo.repo,
            #   run_id: ${{github.run_id}}
            # })

            const timestamp = ${{github.event.head_commit.timestamp}}
            console.log(`Using timestamp ${timestamp} for comparison`)
            const {data} = await github.rest.actions.listWorkflowRunsForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              status: "waiting",
              created: `<${timestamp}`
            })

            console.log(`Found a total of ${data.total_count} pending deployments`)
      - run: TODO - reject each workflow found
